{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run main",
            "type": "debugpy",
            "request": "launch",
            "module": "main.py${file}",
            "python": "${command:python.interpreterPath}",
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
            "console": "integratedTerminal"
        }
    ]
}